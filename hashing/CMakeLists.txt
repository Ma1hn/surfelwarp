file(GLOB HASH_SRC *.h *.hpp *.cuh *.cpp *.cu)
source_group("src" FILES ${HASH_SRC})
cuda_add_library(surfelwarp_hashing_lib ${HASH_SRC})
if(NONRIGID_RECON_MODE STREQUAL "Debug")
    target_link_libraries(surfelwarp_hashing_lib -fsanitize=address -static-libasan)
endif()